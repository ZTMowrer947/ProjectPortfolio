extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      //- Portfolio Heading 
      h1 Greetings, Potential Employers!

      //- Portfolio Description 
      p.lead.text-light
        | This portfolio highlights my wide and various set of programming skills, particularly in Full Stack JavaScript. The projects below demonstrate such skills. Perhaps they can convince you to at least consider me for a job opening?
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      //- Create project cell for each project, each having a thumbnail, title, and link.  
      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            img.thumbnail(src=`${project.image_urls.landing}` alt="Project landing image")
            h5= project.project_name
